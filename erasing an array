def solve():
    n = int(input())
    arr = list(map(int, input().split()))
    
    ops = 1
    for i in range(n - 1):    
        if arr[i] == 1 and arr[i+1] == 0:
            ops += 1
    print(ops)
